import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['localhost','plus.unsplash.com','lh3.googleusercontent.com', 'images.unsplash.com', 'unsplash.com','picsum.photos','app.pinata','salmon-cautious-toucan-412.mypinata.cloud', 'upload.wikimedia.org']

  },
};
export default nextConfig;
